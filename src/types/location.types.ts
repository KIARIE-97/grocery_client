export type TLocation = {
  // ownerType: 'customer' | 'driver' | 'store_owner'
  id: string
  label: string
  email: string
  addressLine1: string
  city: string
  state: string
  postalCode: string
  country: string
}